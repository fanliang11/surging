using System;
using System.Collections.Generic;
using System.Text;
using Microsoft.EntityFrameworkCore;
using System.Linq.Expressions;
using System.Linq;
using Microsoft.EntityFrameworkCore.Query;

 

namespace Repository.EF.Core.Inquiry
{
    //public class InquiryRepository : BaseImpRepository<Customer>
    //{

    //    public override Customer FindBy(Guid key)
    //    {
    //        return _set.Include(a => a.CusInquirys).ThenInclude(b => b.InquiryParameters).Where(a => a.KeyId == key).FirstOrDefault();
    //    }

    //}
}
